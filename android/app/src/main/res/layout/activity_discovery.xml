<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_light"
    android:padding="16dp">

    <!-- Card Stack Container -->
    <FrameLayout
        android:id="@+id/cardStack"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="60dp" />

    <!-- Swipe Feedback -->
    <TextView
        android:id="@+id/likeFeedback"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="LIKE"
        android:textSize="64sp"
        android:textStyle="bold"
        android:textColor="#4CAF50"
        android:alpha="0.7"
        android:visibility="gone" />

    <TextView
        android:id="@+id/dislikeFeedback"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="NOPE"
        android:textSize="64sp"
        android:textStyle="bold"
        android:textColor="#F44336"
        android:alpha="0.7"
        android:visibility="gone" />

    <!-- No More Cards -->
    <TextView
        android:id="@+id/noMoreCardsText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="No more matches!\n\nCheck back later for new book lovers."
        android:textSize="20sp"
        android:textColor="@color/text_secondary"
        android:gravity="center"
        android:visibility="gone" />

    <!-- Instructions -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="16dp"
        android:background="@drawable/name_overlay_background">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="← Swipe left to pass  •  Swipe right to like →"
            android:textSize="14sp"
            android:textColor="@color/text_white"
            android:gravity="center" />
    </LinearLayout>
</FrameLayout>
